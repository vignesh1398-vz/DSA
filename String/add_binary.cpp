#include<bits/stdc++.h>
using namespace std;

string addBinary(string a, string b) {
    char carry = '0';
    string result = "";
    
    for(int i = b.size()-1; i >= 0; i--) {
        if(a[i] == '0') {
            
        }
    }
 }

int main() {
    string a, b;
    cout << "Enter string1: ";
    cin >> a;
    cout << "Enter string2: ";
    cin >> b;

    return 0;
}