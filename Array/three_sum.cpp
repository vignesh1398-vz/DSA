#include <bits/stdc++.h>
#include <iostream>
using namespace std;

vector<int> twoSum(vector<int> &nums, int index)
{
}

vector<vector<int>> threeSum(vector<int> &nums)
{
    for (int i = 0; i < nums.size(); i++)
    {
        vector<int> indices = 
    }
}

int main(int argc, char **argv)
{
    cout << "Hello World";
    return 0;
}
